# Development Profile Configuration
logging:
  level:
    root: INFO
    com.ulpf: DEBUG
    org.springframework.jdbc: DEBUG
jwt:
    secret: ${JWT_SECRET:super-secret-jwt-signing-key-for-ulpf-dev-12345-minimum-256-bits}
spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB
